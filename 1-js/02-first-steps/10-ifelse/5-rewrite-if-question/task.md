importance: 5

---

# Reescreva 'if' usando '?'

Reescreva este `if` usando o operador ternário `'?'`:

```js
let result;

if (a + b < 4) {
  result = 'Menor';
} else {
  result = 'Maior ou igual';
}
```
